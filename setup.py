from os.path import join, dirname, realpath
from setuptools import setup
import sys

setup(
    name='bandu',
    py_modules=['bandu'],
    version=__version__,#'0.1',
    install_requires=[
    ],
    description="",
    author="Richard Li",
)
