from os.path import join, dirname, realpath
from setuptools import setup
import sys

setup(
    name='bandu_v1',
    py_modules=['bandu_v1'],
    version=__version__,#'0.1',
    install_requires=[
    ],
    description="",
    author="Richard Li",
)
